{% set camelCase = config.php.use_camel_case %}
    /**
    * {{ method.description }}
    *
{% for param in method.params %}
    * @param string ${% if camelCase %}{{ config.camel_case[param.name] }}{% else %}{{ param.name }}{% endif %} {{ param.description }}
{% endfor %}
    */
    
    public function {{ method.name }}({% for param in method.params %}${% if camelCase %}{{ config.camel_case[param.name] }}{% else %}{{ param.name }}{% endif %}{% if param.required != "true" %} = ""{% endif %}{% if not loop.last %}, {% endif %}
{% endfor %}) {
        $this->request("{{ method.name }}", array(
{% for param in method.params %}
            '{{ param.name }}' => ${% if camelCase %}{{ config.camel_case[param.name] }}{% else %}{{ param.name }}{% endif %},
{% endfor %}
        ));
    }
    